//src/utils/helpers.js Эта папка предназначена для вспомогательных функций, которые могут использоваться в разных частях бота. Например, функция для форматирования текста:

const formatText = (text) => {
    return text.toUpperCase(); // Пример преобразования текста
};

module.exports = { formatText };